《原神·星铁》智能旅伴 功能开发计划书
定位：Windows 11 桌面小工具，围绕截图、抽卡记录、游戏时长构建个人游戏生涯管理。
原则：绝不读取/修改游戏内存，不注入进程，不访问网络（除可选云同步外），所有数据本地处理。

一、核心功能模块（MVP）
1. 抽卡记录自动解析与本地战绩库
输入：用户对游戏内“抽卡记录”页面的截图（可单张，也可连续多张）
处理流程：

画面特征识别

检测截图是否包含抽卡记录列表：

原神：识别“历史记录”标题 + 纠缠之缘/相遇之缘图标

星铁：识别“跃迁记录”标题 + 星轨通票/星轨专票图标

可通过裁剪特征区域、模板匹配或简单的颜色直方图判断（使用 image 库）。

区域切分

将每一行记录（日期、物品名称、星级）切出来，形成待 OCR 的小图块。

可通过固定比例切割或基于分隔线检测（如淡色背景行）。

OCR 文字提取

使用 Windows 内置 OCR 引擎（Windows.Media.Ocr），通过 windows-rs 调用。

获得日期、物品名、星级字符串。

对物品名做简单规范化（如“七七” “七七·角色” 统一为“七七”）。

去重与入库

将解析结果与本地 SQLite 数据库比对，按 日期+物品+星级 去重。

新增记录时写入。

本地战绩馆 UI

列表视图：按时间倒序展示所有抽卡记录。

统计卡片：累计抽数、5星数量、平均出货间隔、当前保底计数（可由算法推断）。

欧非曲线：横轴为抽数，纵轴为出货标识，可视化展示。

歪卡纪念墙：筛选出“常驻池角色/光锥”的中彩记录，单独陈列。

数据导出：支持导出为 CSV/JSON，可导入其他第三方分析工具。

存储占用：每条记录约 50 字节，10 万条约 5 MB。

2. 游戏时长与活跃度记录
原理：周期扫描进程列表，匹配 YuanShen.exe、StarRail.exe。
前台检测：获取当前前台窗口的进程名，判断是否为游戏进程，仅在前台时计时（避免挂机虚高）。
记录粒度：按天、按版本周期统计。
版本周期定义：内置或手动编辑游戏版本起止日期表（可从米游社公告抓取，或用户自行输入）。
可视化：

日/周/月热力图（类似 GitHub 贡献图）

各版本平均在线时长对比柱状图
数据：完全本地，单条记录几十字节，年数据 <1 MB。

3. 截图智能策展（在现有管理之上增强）
你已有截图管理，我们在此基础上加入：

自动标签生成

抽卡截图 → 自动标记为“抽卡记录”

场景图：OCR 识别截图底部/顶部的区域名称文字（如“璃月港”“黑塔空间站”），生成场景标签。

成就弹出：识别成就名称文字，标记为“成就”。

角色面板识别（可选扩展）

检测角色属性面板截图，OCR 提取关键数值（攻击、暴击、暴伤等），生成角色练度卡片。

智能搜索

在工具内搜索“深渊”，即可列出所有场景包含“深境螺旋”或成就含“深渊”的截图。

搜索“七七”可找到所有含七七的抽卡记录截图。

实现：对新增截图在后台异步处理，不阻塞主线程。
索引存储：每条截图元数据 <1 KB，1 万张约 10 MB 内。

4. 体力/派遣轻提醒（辅助）
用户预设循环提醒（如每18小时弹通知）。

利用 Windows Toast 通知 (windows-rs 的 ToastNotification)，点击可直接启动游戏（通过 shell 执行游戏可执行文件）。

不加任何游戏进程读取，纯粹定时器。

二、技术栈与关键依赖
组件	推荐方案	Rust 依赖/调用
图形界面	基于你现有工具框架（若为 egui/Tauri/Druid 等）；若可改，推荐 Tauri + HTML/CSS 呈现图表	沿用或重构
OCR	Windows.Media.Ocr（UWP API，无需联网，支持中文）	windows crate（Windows::Media::Ocr）
图像特征/模板匹配	轻量模板匹配确定截图类型	image + imageproc
数据库	SQLite	rusqlite
进程监控	每秒 tick 扫描，前台窗口检测	sysinfo + windows crate (GetForegroundWindow, GetWindowThreadProcessId)
通知	Windows 10/11 Toast	windows crate (Windows::UI::Notifications)
图表	若用 Tauri/WebView，用 Chart.js；若原生 GUI，可用 plotters 绘图	前端或 plotters
三、数据流与文件结构
text
AppData/
├── genshin_starrail.db          # SQLite：抽卡记录、游戏时长、版本定义
├── screenshot_index.db          # 截图元数据（路径、标签、OCR 文本等）
├── config.json                  # 用户设置（提醒间隔、版本日期）
└── thumbnails/                  # 可选缩略图缓存（小尺寸）
所有数据都存于工具自己的目录，卸载直接删除即可。

四、安全与隐私红线（重要！）
绝不读取/写入游戏内存，不注入 DLL，不 hook API。

不模拟键盘鼠标，不自动操作游戏。

仅在用户手动截图后对图片进行 OCR，不自动截取游戏画面。

网络请求仅当用户主动使用云同步功能时才发生，其他功能完全离线。

进程名和窗口标题检测属于正常系统调用，不被反作弊系统禁止。
以上设计保证了 100% 不触及《原神》《星铁》的封号红线。

五、MVP 迭代分步执行
建议优先完成的三个“炸弹级”功能：

抽卡记录截图解析 + 本地战绩库（核心差异化，可炫耀）

智能截图标签与搜索（让截图库立刻可检索）

游戏时长热力图 + 版本周期统计（黏性功能）

后续再补：

角色面板练度 OCR

云同步（WebDAV/OneDrive 支持）

更丰富的统计报表（资源台账等）

六、可以喂给 AI 的开发提示示例
若你要把计划交给 AI 编码助手，可这样开头：

请使用 Rust 开发一个 Windows 11 本地工具的功能模块：抽卡记录截图解析。
输入为用户提供的《原神》或《崩坏：星穹铁道》抽卡记录界面截图。

先用 image 库加载图片，通过模板匹配或颜色特征判断是否属于抽卡记录页（特征：原神有“历史记录”标题+纠缠之缘图标，星铁有“跃迁记录”+星轨票）。

识别后，将截图按固定比例裁剪出行项小图。

使用 windows-rs 调用 Windows.Media.Ocr.OcrEngine 识别中文文字，提取日期、物品名、星级。

将结果去重后存入 SQLite 数据库。

提供函数接口，返回解析到的记录列表。
注意代码要模块化，OCR 调用要在异步任务中执行，并给出详细注释。

这份计划书可直接复制给 AI 工具，逐模块开发。若你在开发中遇到任何细节问题（比如抽卡截图的具体坐标比例、OCR 优化等），随时可以进一步追问，我会提供更细粒度的技术方案。